# name: "Go chat-app's API Tests"

# on:
#   push:
#     branches: [main] # Runs on main and any refactor branch
#   pull_request:
#     branches: [main]

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.25.5" # Use the latest Go version

#       - name: Install dependencies
#         run: go mod download

#       - name: Run Tests
#         run: go test -v ./... # Runs all tests, including your mockery-based ones
